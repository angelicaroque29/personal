import React from 'react'
import ReactDOM from 'react-dom/client'
import BuyerReadinessReport from './BuyerReadinessReport'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BuyerReadinessReport />
  </React.StrictMode>
)
